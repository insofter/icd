if [ -f /etc/bash_completion ] && ! shopt -oq posix; then
    . /etc/bash_completion
fi

export ICD_SQL_DIR="$HOME/projects/icd/sql"
export ICD_DATA_DIR="$HOME/projects/icd"
export ICD_TEMP_DIR="$HOME/projects/icd"
export PATH="$PATH:$HOME/projects/icd"
export PATH="$PATH:$HOME/projects/icd/scripts"

